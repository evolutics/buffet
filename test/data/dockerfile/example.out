ARG example=''
ARG example_foo='a'

ARG _alpine_version='3.9.4'

FROM alpine AS example
ARG example
ARG example_bar='b'
RUN if [[ -n "${example}" ]]; then \
    echo "${example}" \
  ; fi

FROM alpine:"${_alpine_version}"

ARG example
ARG example_baz='c'
RUN if [[ -n "${example}" ]]; then \
    ls \
  ; fi

WORKDIR /workdir
