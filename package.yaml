name: buffet
version: "0.1.0.0"
github: "evolutics/buffet"
license: MIT
author: "Benjamin Fischer"
maintainer: "benjamin.fischer@evolutics.info"
copyright: "2019 Benjamin Fischer"
extra-source-files:
  - README.md
# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Development
# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description: Please see the README on GitHub at <https://github.com/evolutics/buffet#readme>
dependencies:
  - base >= 4.7 && < 5
_ghc-options: &ghc-options
  - -threaded
  - -rtsopts
  - -with-rtsopts=-N
  - -Werror
  - -Weverything
  - -Wno-unsafe
  - -Wno-safe
data-files:
  - data/document/default_template.md.mustache
internal-libraries:
  buffet-internal:
    source-dirs: src
    ghc-options: *ghc-options
    dependencies:
      - aeson
      - bytestring
      - cassava
      - containers
      - directory
      - filepath
      - language-docker
      - mustache
      - optparse-applicative
      - split
      - text
      - typed-process
      - vector
      - yaml
    other-modules:
      - Paths_buffet
    generated-other-modules:
      - Paths_buffet
executables:
  buffet-exe:
    main: Main.hs
    source-dirs: app
    ghc-options: *ghc-options
    dependencies:
      - buffet-internal
tests:
  buffet-test:
    main: Spec.hs
    source-dirs: test
    ghc-options: *ghc-options
    dependencies:
      - aeson
      - buffet-internal
      - directory
      - filepath
      - tasty
      - tasty-golden
      - tasty-hunit
      - text
      - typed-process
      - unordered-containers
